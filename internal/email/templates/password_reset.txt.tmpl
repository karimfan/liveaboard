Someone requested a password reset for your {{.AppName}} account ({{.RecipientEmail}}).

If that was you, set a new password:

  {{.ActionURL}}

This link expires on {{fmtdate .ExpiresAt}}.

If you didn't request a reset, you can ignore this message — your
password won't change.

— {{.AppName}}
