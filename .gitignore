# macOS
.DS_Store

# Go build artifacts
/bin/
*.test
*.out
coverage.txt

# Local environment — secrets
.env
.env.local
web/.env.local

# Per-developer deploy config (see env.sh.example). Contains GCP project
# info AND SMTP credentials — never commit.
env.sh

# Frontend
web/node_modules/
web/dist/
!web/dist/.keep
web/.vite/
web/tsconfig.tsbuildinfo

# Editor noise
.idea/
.vscode/

# Tooling
.gstack/
.claude/
